const express = require('express');
const router = express.Router();

// GET /library (see list of novels) index
router.get('');
// GET /library/new (add a novel to mylibrary) new
router.get('');
// GET/library/:id (see 1 novel)
router.get('');
// GET /library/:id (make a my library)





module.exports = router;